{
  "name": "ecce-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for ecce - Claude Code management tools",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ecce-mcp-server": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run dist/index.js",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
